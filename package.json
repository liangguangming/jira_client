{
  "name": "nradio_cloud_jira_client",
  "version": "1.0.0",
  "description": "A project named nradio_cloud_jira_client",
  "main": "src/app.js",
  "scripts": {
    "test": "echo \"Error: no test specified\" && exit 1",
    "dev": "nodemon --watch src --watch .env src/bin/dev.js",
    "lint-fix": "eslint --ext .js ./src --fix",
    "lint": "eslint --ext .js ./src"
  },
  "dependencies": {
    "cron": "^1.8.2",
    "dotenv": "^8.2.0",
    "jira-client": "^6.20.0",
    "moment": "^2.28.0",
    "request": "^2.88.2",
    "request-promise": "^4.2.6"
  },
  "devDependencies": {
    "nodemon": "^2.0.4",
    "eslint": "^7.2.0",
    "eslint-config-airbnb-base": "^14.2.0",
    "eslint-plugin-import": "^2.21.2",
    "husky": "^4.2.5"
  },
  "author": "",
  "license": "ISC",
  "husky": {
    "hooks": {
      "pre-commit": "npm run lint"
    }
  }
}
